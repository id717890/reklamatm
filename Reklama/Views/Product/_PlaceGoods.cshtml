@{
    Layout = null;
}

@if (User.Identity.IsAuthenticated)
{
    <div class="placeGoods">
        @if(User.IsInRole("Shop")) {
            @Html.ActionLink(" ", "RedirectToBaseProducts", "Shop")
        }
        else {
            @Html.ActionLink(" ", "Create", "Shop")
        }
    </div>
}