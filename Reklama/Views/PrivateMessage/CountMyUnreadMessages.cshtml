@model System.Int32

@{
    Layout = null;
}

@if(Model > 0)
{
    <span>@Html.ActionLink(Model.ToString(), "Index", "PrivateMessage", null, new Dictionary<string, object>() { { "class", "CountOfMyPrivateMessages" } })</span>
}